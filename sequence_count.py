#defining the sequence counting function
def print_sequence_length():
    seq = 'AGTGTCCCTG' #Storing DNA strand as a string in the 'seq' variable
    print(len(seq)) #prints the length of the sequence stored in 'seq'

if __name__ == "__main__": #telling python to run the code below
  print_sequence_length() #run our functionpw
